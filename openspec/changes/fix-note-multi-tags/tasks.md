## 1. 准备工作

- [x] 1.1 阅读现有代码，理解 Note 创建/更新流程
- [x] 1.2 确认数据库表结构正确（note_tags 表的复合主键）

## 2. Repository 层修改

- [x] 2.1 在 `repository/note.go` 中添加事务版本的 `CreateWithTags` 方法
- [x] 2.2 在 `repository/note.go` 中添加事务版本的 `UpdateWithTags` 方法
- [x] 2.3 确保事务正确回滚

## 3. Handler 层修改

- [x] 3.1 修改 `handlers/note.go` 的 `Create` 方法，使用事务保存
- [x] 3.2 修改 `handlers/note.go` 的 `Update` 方法，使用事务保存
- [x] 3.3 添加错误处理，标签保存失败时返回 500 错误
- [x] 3.4 添加日志记录（创建开始、标签保存失败等关键点）

## 4. Copy 方法修复

- [x] 4.1 修改 `handlers/note.go` 的 `Copy` 方法，确保复制时标签也使用事务

## 5. 测试验证

- [x] 5.1 运行现有的单元测试确保没有回归
- [ ] 5.2 手动测试：创建带多个标签的笔记
- [ ] 5.3 手动测试：更新笔记添加多个标签
- [ ] 5.4 手动测试：刷新页面后验证标签数量正确
- [ ] 5.5 验证错误场景（如标签过长）返回正确的错误信息

## 6. 清理

- [x] 6.1 删除或更新不再需要的注释
- [x] 6.2 确保代码风格一致
